<li>
	{{burger_name}}

	<button class="btn waves-effect green btn-large" type="submit" name="action" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}ORDER AGAIN{{/if}}
	</button>

	<button class="btn waves-effect red lighten-1 btn-large" type="submit" name="action" data-id="{{this.id}}">DELETE</button>
</li>